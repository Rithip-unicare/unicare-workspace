$kind: http-request
url: "{{baseUrl}}/admin/consentform/dental-chart-disable?{{formId}}?id={{formId}}"
method: PUT
headers:
  Authorization: Bearer {{accesstoken}}
queryParams:
  - key: id
    value: "{{formId}}"
    description: Sent as query param
auth:
  type: noauth
order: 6000

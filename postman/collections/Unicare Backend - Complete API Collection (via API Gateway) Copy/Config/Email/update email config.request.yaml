$kind: http-request
url: "{{baseUrl}}/api/organization/email-config"
method: PUT
body:
  type: json
  content: |-
    {
        "id":1,
      "organizationId": 1,
      "host": "smtp",
      "port": 587,
      "userName": "string",
      "password": "string",
      "useAuth": true,
      "protocol": "stms"
    }
auth:
  type: bearer
order: 3000

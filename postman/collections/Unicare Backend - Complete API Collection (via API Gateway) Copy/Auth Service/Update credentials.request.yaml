$kind: http-request
url: "{{baseUrl}}/api/auth/update-credentials"
method: PUT
body:
  type: json
  content: |-
    {
        "userId":"2",
        "username":"partm",
        "password":"parthi"
    }
auth:
  type: bearer
order: 16000

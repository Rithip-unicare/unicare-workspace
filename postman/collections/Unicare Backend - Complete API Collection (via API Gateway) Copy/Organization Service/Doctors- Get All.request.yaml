$kind: http-request
name: "Doctors: Get All"
url: "{{baseUrl}}/api/organization/doctors/1"
method: GET
headers:
  - key: Authorization
    value: Bearer {{accessToken}}
    disabled: true
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 26000

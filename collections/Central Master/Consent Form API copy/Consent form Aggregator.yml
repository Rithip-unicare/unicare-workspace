info:
  name: Consent form Aggregator
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}/api/admin/consentform-masters"
  headers:
    - name: Authorization
      value: Bearer {{accesstoken}}
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: localhost:9001

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

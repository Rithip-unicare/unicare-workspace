info:
  name: Get All Role Permissions
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/organization/role-module-permissions"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

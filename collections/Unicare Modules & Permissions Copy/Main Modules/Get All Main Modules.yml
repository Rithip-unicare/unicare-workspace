info:
  name: Get All Main Modules
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/organization/main-modules"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

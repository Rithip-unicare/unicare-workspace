info:
  name: Delete User Module Permission
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/organization/user-module-permissions/:id"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get All User Module Permissions
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/organization/user-module-permissions/user/31"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get User Module Permission By Id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/organization/user-module-permissions/:id"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

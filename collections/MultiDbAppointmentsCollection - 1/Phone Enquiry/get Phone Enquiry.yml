info:
  name: get Phone Enquiry
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/phone-enquiry/ALB1/25865"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get Appointments
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/appointments/getAll"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
          "location":"ALB1",
          "doctorId":1,
          "apptDate":"2024-07-26"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

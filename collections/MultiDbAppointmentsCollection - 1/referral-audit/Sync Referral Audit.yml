info:
  name: Sync Referral Audit
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/referral-audit/sync"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "Generated from cURL: curl -X POST {{baseUrl}}/api/referral-audit/sync"

info:
  name: all locatiom
  type: http
  seq: 14

http:
  method: POST
  url: "{{baseUrl}}/api/referrals/all-branches?size=20"
  params:
    - name: size
      value: "20"
      type: query
  body:
    type: json
    data: |-
      {
        "page":0,
        "size":1000
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: 'Generated from cURL: curl -X GET "{{baseUrl}}/api/referral-audit/logs?userId=&operation=&fromDate=&toDate=&location=&incompleteOnly="'

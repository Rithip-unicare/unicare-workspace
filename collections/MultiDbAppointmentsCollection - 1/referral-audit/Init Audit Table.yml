info:
  name: Init Audit Table
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/referral-audit/init-table"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "Generated from cURL: curl -X POST {{baseUrl}}/api/referral-audit/init-table"

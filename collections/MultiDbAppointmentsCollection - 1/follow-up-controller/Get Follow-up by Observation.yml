info:
  name: Get Follow-up by Observation
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/followup/observation/{{location}}/{{observationId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get a specific follow-up record by observation ID at a specific location.

info:
  name: Sync Follow-up Audit
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/api/followup/sync-audit"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Sync follow-up audit data.

info:
  name: Get Follow-ups by Patient (All Branches)
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/followup/patient/all-branches/{{patientId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get follow-up records for a patient across all branches.

info:
  name: Get Appointment Status
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/masters/appointment-status"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

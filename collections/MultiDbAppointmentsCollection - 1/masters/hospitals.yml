info:
  name: hospitals
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/masters/hospitals"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

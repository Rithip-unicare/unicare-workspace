info:
  name: Get All doctors from one location using department id
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/doctors/department/ALB5/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get All doctors using departmentID in all branch
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/doctors/department/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

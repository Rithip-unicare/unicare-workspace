info:
  name: Get Waiting List Stats
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/waiting-list-audit/stats"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get waiting list user performance statistics across branches.

info:
  name: Get Stats by Location
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/waiting-list-audit/stats/location"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get waiting list user performance statistics for a specific branch with date range.

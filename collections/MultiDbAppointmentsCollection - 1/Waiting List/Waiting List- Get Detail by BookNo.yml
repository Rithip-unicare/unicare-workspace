info:
  name: "Waiting List: Get Detail by BookNo"
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/waiting-list/{{location}}/detail/{{bookNo}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: 'Generated from cURL: curl -X GET "{{baseUrl}}/api/waiting-list/{{location}}/detail/{{bookNo}}"'

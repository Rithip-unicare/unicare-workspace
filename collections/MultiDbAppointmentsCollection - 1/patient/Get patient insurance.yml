info:
  name: Get patient insurance
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/patient/insurance/ALB1/30318"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

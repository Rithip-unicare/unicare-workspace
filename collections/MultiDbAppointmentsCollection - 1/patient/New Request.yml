info:
  name: New Request
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/patient/waiting-and-followup/{{location}}/{{patientId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

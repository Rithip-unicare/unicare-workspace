info:
  name: Get Appointment Audit Logs
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/core/appointments/1/audit-logs"
  headers:
    - name: X-User-Id
      value: "{{userId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

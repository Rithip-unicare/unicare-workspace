info:
  name: DoctorDailyAppointments
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/api/core/appointments/doctors-daily-appointments/1/50?date=2026-06-05"
  params:
    - name: date
      value: 2026-06-05
      type: query
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

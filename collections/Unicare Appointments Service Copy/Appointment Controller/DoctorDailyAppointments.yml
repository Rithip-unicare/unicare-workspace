info:
  name: DoctorDailyAppointments
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/api/core/appointments/doctors-daily-appointments/5/1?date=2026-08-05&organizationId=2"
  params:
    - name: date
      value: 2026-08-05
      type: query
    - name: organizationId
      value: "2"
      type: query
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

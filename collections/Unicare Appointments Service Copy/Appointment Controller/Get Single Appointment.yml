info:
  name: Get Single Appointment
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/core/appointments/5"
  headers:
    - name: X-User-Id
      value: "{{userId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

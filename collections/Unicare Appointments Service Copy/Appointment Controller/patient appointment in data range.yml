info:
  name: patient appointment in data range
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}/api/core/appointments/patient-appointments-by-date-range"
  body:
    type: json
    data: |-
      {
        "patientDetailId": 1,
        "hospitalId": 1,
        "startDate": "2026-04-15",
        "endDate": "2026-06-29"
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Delete Appointment
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/api/core/appointments/2"
  headers:
    - name: X-User-Id
      value: "{{userId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

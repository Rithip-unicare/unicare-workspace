info:
  name: View Appointments
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/api/core/appointments/view"
  headers:
    - name: X-User-Id
      value: "{{userId}}"
      disabled: true
    - name: Authorization
      value: "Bearer {{accessToken}} "
  body:
    type: json
    data: |-
      {
          "doctorIds": [
              2],
          "hospitalDetailId": 3,
          "startDate": "2026-03-05",
          "endDate": "2026-08-05"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get Patient Appointment History
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/core/appointments/patient-history/1/1"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

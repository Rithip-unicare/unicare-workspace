info:
  name: Get Doctor Block By Id
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/core/doctor-blocks/1/3"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

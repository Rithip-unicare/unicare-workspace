info:
  name: Delete Enquiry
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/core/patient-enquiries/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

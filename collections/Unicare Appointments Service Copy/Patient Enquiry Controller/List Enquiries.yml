info:
  name: List Enquiries
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/core/patient-enquiries?hospitalId=1"
  params:
    - name: hospitalId
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

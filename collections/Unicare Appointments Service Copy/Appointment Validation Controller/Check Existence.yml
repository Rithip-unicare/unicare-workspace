info:
  name: Check Existence
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/core/appointments/validation/existence"
  headers:
    - name: X-User-Id
      value: "{{userId}}"
  body:
    type: json
    data: |-
      {
          "doctorId": 1,
          "hospitalDetailId": 1,
          "appointmentDate": "2026-05-15",
          "startTime": "10:00:00",
          "endTime": "10:15:00"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

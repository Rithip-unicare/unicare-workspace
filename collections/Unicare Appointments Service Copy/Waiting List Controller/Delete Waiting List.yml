info:
  name: Delete Waiting List
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/waiting-list/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

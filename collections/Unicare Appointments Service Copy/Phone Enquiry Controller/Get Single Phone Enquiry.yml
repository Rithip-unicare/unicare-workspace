info:
  name: Get Single Phone Enquiry
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/core/phone-enquiries/13"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

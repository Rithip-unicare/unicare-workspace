info:
  name: Delete Phone Enquiry
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/api/core/phone-enquiries/1"
  headers:
    - name: X-User-Id
      value: "{{userId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

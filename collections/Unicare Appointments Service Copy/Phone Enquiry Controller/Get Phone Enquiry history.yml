info:
  name: Get Phone Enquiry history
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/core/phone-enquiries/gethistory/1?primaryNumber=string"
  params:
    - name: primaryNumber
      value: string
      type: query
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get Logs
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/core/appointments/audit/1"
  headers:
    - name: X-User-Id
      value: "{{userId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get AuditLog by Entry id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/core/audit-logs/phone-enquiry/1/1"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

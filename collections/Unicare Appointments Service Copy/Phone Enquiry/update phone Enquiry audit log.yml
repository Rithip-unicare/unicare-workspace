info:
  name: update phone Enquiry audit log
  type: http
  seq: 2

http:
  method: PUT
  url: "{{baseUrl}}/api/core/audit-logs/phone-enquiry/update"
  body:
    type: json
    data: |-
      {
        "auditLogId": 2,
        "description": "Phone Enquiry Created 123",
        "eventTime": "2026-06-01T16:27:32"
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

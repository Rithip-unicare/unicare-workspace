info:
  name: Resend Login OTP
  type: http
  seq: 19

http:
  method: POST
  url: "{{baseUrl}}/api/auth/resend-login-otp"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "userId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

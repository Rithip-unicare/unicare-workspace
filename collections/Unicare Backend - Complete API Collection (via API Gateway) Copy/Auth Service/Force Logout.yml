info:
  name: Force Logout
  type: http
  seq: 13

http:
  method: POST
  url: "{{baseUrl}}/api/auth/force-logout?userId=1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: userId
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

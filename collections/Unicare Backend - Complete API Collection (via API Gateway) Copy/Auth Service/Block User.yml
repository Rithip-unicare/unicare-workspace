info:
  name: Block User
  type: http
  seq: 10

http:
  method: POST
  url: "{{baseUrl}}/api/auth/block-user?userId=4"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: userId
      value: "4"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

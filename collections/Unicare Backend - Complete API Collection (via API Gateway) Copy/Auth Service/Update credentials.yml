info:
  name: Update credentials
  type: http
  seq: 16

http:
  method: PUT
  url: "{{baseUrl}}/api/auth/update-credentials"
  body:
    type: json
    data: |-
      {
          "userId":"17",
          "username":"rithipnew",
          "password":"1234567890"
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Active Sessions
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/auth/sessions?userId=1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: userId
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

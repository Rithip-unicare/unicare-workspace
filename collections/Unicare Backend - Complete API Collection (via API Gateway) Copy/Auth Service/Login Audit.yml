info:
  name: Login Audit
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseUrl}}/api/auth/login-audit/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

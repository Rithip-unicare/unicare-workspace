info:
  name: get sms config by organizations
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/organization/sms-config/organization/1"
  body:
    type: text
    data: ""
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

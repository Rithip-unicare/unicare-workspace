info:
  name: get Email config by organization
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/organization/email-config/organization/1"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

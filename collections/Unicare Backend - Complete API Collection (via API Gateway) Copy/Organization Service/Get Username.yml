info:
  name: Get Username
  type: http
  seq: 53

http:
  method: GET
  url: "{{baseUrl}}/api/organization/users/username/10"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

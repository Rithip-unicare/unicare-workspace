info:
  name: "Doctors: Get All"
  type: http
  seq: 26

http:
  method: GET
  url: "{{baseUrl}}/api/organization/doctors/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

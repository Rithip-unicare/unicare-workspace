info:
  name: GetUsrThemeSetting by userId and OrgId
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/organization/user-theme-settings/1/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: "Users: Delete"
  type: http
  seq: 13

http:
  method: DELETE
  url: "{{baseUrl}}/api/organization/users/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: "User Info: Get All by Organization Id"
  type: http
  seq: 41

http:
  method: GET
  url: "{{baseUrl}}/api/organization/user-info/organization/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Organization Resend OTP
  type: http
  seq: 56

http:
  method: POST
  url: "{{baseUrl}}/api/organization/auth/resend-otp"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "userId": 67
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

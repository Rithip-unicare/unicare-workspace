info:
  name: "Files: Get"
  type: http
  seq: 31

http:
  method: GET
  url: "{{baseUrl}}/api/organization/files/USER/1/PROFILE_PIC"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

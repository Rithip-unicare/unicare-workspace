info:
  name: GetSideMenuList
  type: http
  seq: 45

http:
  method: GET
  url: "{{baseUrl}}/api/organization/user-module-permissions/sideMenuAllList"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

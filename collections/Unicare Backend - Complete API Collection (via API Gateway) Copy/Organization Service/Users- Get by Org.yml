info:
  name: "Users: Get by Org"
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/organization/users/organization/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

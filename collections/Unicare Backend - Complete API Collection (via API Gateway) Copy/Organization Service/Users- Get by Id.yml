info:
  name: "Users: Get by Id"
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/api/organization/users/6"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

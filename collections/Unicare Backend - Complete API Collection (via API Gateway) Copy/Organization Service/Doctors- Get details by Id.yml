info:
  name: "Doctors: Get details by Id"
  type: http
  seq: 27

http:
  method: GET
  url: "{{baseUrl}}/api/organization/doctors/details/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

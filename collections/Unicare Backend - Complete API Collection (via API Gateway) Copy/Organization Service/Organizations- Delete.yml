info:
  name: "Organizations: Delete"
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/api/organization/organizations/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

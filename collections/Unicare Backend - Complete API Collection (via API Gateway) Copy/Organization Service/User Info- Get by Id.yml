info:
  name: "User Info: Get by Id"
  type: http
  seq: 39

http:
  method: GET
  url: "{{baseUrl}}/api/organization/user-info/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

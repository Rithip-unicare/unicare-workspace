info:
  name: "Organizations: Get by Id"
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/organization/organizations/8"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: "Roles: Get by Org"
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseUrl}}/api/organization/roles"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: GetRoleWiseMainMenu
  type: http
  seq: 51

http:
  method: GET
  url: "{{baseUrl}}/api/organization/role-module-permissions/GetRoleWiseMainMenu/1"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

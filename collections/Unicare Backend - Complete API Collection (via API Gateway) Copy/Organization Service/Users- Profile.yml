info:
  name: "Users: Profile"
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/api/organization/users/profile/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

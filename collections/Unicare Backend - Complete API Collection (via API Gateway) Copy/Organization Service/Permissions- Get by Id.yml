info:
  name: "Permissions: Get by Id"
  type: http
  seq: 22

http:
  method: GET
  url: "{{baseUrl}}/api/organization/permissions/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

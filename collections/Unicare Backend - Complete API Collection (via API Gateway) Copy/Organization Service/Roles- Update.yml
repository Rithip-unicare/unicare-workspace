info:
  name: "Roles: Update"
  type: http
  seq: 17

http:
  method: PUT
  url: "{{baseUrl}}/api/organization/roles"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "id": 1,
        "roleName": "DOCTOR_ROLE",
        "description": "Updated desc"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

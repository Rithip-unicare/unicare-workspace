info:
  name: "Storage: Get Config"
  type: http
  seq: 32

http:
  method: GET
  url: "{{baseUrl}}/api/organization/storage-config"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

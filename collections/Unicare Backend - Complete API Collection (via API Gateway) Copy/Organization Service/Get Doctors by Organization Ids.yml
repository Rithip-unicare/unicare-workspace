info:
  name: Get Doctors by Organization Ids
  type: http
  seq: 55

http:
  method: GET
  url: "{{baseUrl}}/api/organization/doctors/by-organizations?orgIds=1,2"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: orgIds
      value: 1,2
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

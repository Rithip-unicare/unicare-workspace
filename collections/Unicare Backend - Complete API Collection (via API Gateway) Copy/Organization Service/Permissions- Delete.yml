info:
  name: "Permissions: Delete"
  type: http
  seq: 25

http:
  method: DELETE
  url: "{{baseUrl}}/api/organization/permissions/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

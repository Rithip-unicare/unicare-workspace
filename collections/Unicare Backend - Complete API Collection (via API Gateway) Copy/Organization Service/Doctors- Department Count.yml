info:
  name: "Doctors: Department Count"
  type: http
  seq: 29

http:
  method: GET
  url: "{{baseUrl}}/api/organization/doctors/department-count/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: "Organization Settings: Get by Organization Id"
  type: http
  seq: 35

http:
  method: GET
  url: "{{baseUrl}}/api/organization/organization-settings/organization/3"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

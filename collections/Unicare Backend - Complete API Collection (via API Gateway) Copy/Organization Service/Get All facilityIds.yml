info:
  name: Get All facilityIds
  type: http
  seq: 57

http:
  method: GET
  url: "{{baseUrl}}/api/organization/organizations/authorities"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

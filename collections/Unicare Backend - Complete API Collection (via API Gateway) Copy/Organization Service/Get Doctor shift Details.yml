info:
  name: Get Doctor shift Details
  type: http
  seq: 54

http:
  method: GET
  url: "{{baseUrl}}/api/organization/doctors/shift-details/50"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: "Master: Get Relationships"
  type: http
  seq: 16

http:
  method: GET
  url: "{{baseUrl}}/api/master/relationships"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

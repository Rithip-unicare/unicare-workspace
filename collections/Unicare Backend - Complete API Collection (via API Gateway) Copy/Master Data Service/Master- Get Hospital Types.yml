info:
  name: "Master: Get Hospital Types"
  type: http
  seq: 29

http:
  method: GET
  url: "{{baseUrl}}/api/master/hospital-types"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

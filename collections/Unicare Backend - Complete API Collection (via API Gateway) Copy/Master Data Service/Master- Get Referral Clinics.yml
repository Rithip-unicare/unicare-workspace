info:
  name: "Master: Get Referral Clinics"
  type: http
  seq: 45

http:
  method: GET
  url: "{{baseUrl}}/api/master/referral-clinics"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

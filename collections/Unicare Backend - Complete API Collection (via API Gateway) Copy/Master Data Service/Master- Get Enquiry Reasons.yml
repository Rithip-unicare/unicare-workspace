info:
  name: "Master: Get Enquiry Reasons"
  type: http
  seq: 41

http:
  method: GET
  url: "{{baseUrl}}/api/master/enquiry-reasons"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Demographic Aggregator
  type: http
  seq: 58

http:
  method: GET
  url: "{{baseUrl}}/api/master/insurance-companies/main-company"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: get insurance company by search
  type: http
  seq: 55

http:
  method: GET
  url: "{{baseUrl}}/api/master/insurance-companies/search?searchTerm=IC-AAFIYA-96"
  params:
    - name: searchTerm
      value: IC-AAFIYA-96
      type: query
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: "Master: Get Nationalities"
  type: http
  seq: 13

http:
  method: GET
  url: "{{baseUrl}}/api/master/nationalities"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: "Master: Get Enquiry Services"
  type: http
  seq: 42

http:
  method: GET
  url: "{{baseUrl}}/api/master/enquiry-services"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

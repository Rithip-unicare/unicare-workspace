info:
  name: "Master: Get Languages"
  type: http
  seq: 20

http:
  method: GET
  url: "{{baseUrl}}/api/master/languages"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

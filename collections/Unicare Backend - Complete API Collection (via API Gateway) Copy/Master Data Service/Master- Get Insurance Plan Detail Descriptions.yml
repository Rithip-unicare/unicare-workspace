info:
  name: "Master: Get Insurance Plan Detail Descriptions"
  type: http
  seq: 40

http:
  method: GET
  url: "{{baseUrl}}/api/master/insurance-plan-detail-descriptions"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

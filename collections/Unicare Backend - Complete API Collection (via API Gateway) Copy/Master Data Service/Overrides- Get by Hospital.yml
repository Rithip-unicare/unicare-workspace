info:
  name: "Overrides: Get by Hospital"
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/master/overrides/hospital-uuid-1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

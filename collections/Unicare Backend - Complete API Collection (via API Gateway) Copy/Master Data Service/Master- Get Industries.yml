info:
  name: "Master: Get Industries"
  type: http
  seq: 22

http:
  method: GET
  url: "{{baseUrl}}/api/master/industries"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: "Master: Get Abuse Types"
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/master/abuse-types"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

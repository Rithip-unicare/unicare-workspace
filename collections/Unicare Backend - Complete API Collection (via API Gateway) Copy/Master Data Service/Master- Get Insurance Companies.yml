info:
  name: "Master: Get Insurance Companies"
  type: http
  seq: 35

http:
  method: GET
  url: "{{baseUrl}}/api/master/insurance-companies?page=0&size=10"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: page
      value: "0"
      type: query
    - name: size
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

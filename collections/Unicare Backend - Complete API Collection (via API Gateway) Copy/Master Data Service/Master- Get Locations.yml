info:
  name: "Master: Get Locations"
  type: http
  seq: 30

http:
  method: GET
  url: "{{baseUrl}}/api/master/locations"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

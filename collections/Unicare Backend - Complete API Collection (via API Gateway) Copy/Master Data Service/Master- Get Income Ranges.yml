info:
  name: "Master: Get Income Ranges"
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/api/master/income-ranges"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

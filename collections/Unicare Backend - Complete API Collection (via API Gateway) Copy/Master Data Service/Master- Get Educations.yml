info:
  name: "Master: Get Educations"
  type: http
  seq: 21

http:
  method: GET
  url: "{{baseUrl}}/api/master/educations"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

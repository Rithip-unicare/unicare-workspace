info:
  name: Insurance
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/master/insurance/aggregator"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

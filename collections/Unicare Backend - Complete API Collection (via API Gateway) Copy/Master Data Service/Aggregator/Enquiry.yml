info:
  name: Enquiry
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/master/enquiry/aggregator"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

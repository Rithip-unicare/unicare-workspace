info:
  name: Refferal
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/master/refferal/aggregator"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

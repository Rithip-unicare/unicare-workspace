info:
  name: Appointments
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/master/appointments/aggregator"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

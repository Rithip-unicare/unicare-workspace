info:
  name: "Master: Get Service Types"
  type: http
  seq: 32

http:
  method: GET
  url: "{{baseUrl}}/api/master/service-types"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

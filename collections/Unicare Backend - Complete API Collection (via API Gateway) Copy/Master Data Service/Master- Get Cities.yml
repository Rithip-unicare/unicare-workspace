info:
  name: "Master: Get Cities"
  type: http
  seq: 24

http:
  method: GET
  url: "{{baseUrl}}/api/master/cities"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

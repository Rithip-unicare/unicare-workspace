info:
  name: Get Insurance Main company
  type: http
  seq: 57

http:
  method: GET
  url: "{{baseUrl}}/api/master/insurance/insurance-companies/main-company"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: "Master: Get Blood Groups"
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/api/master/blood-groups"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

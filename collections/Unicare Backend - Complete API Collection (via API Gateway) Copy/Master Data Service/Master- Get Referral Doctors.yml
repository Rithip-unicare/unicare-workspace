info:
  name: "Master: Get Referral Doctors"
  type: http
  seq: 46

http:
  method: GET
  url: "{{baseUrl}}/api/master/referral-doctors"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

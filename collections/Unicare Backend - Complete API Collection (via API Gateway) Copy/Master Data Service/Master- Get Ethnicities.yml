info:
  name: "Master: Get Ethnicities"
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/api/master/ethnicities"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: "Master: Get Referral Sources"
  type: http
  seq: 44

http:
  method: GET
  url: "{{baseUrl}}/api/master/referral-sources"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

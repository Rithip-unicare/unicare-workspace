info:
  name: Delete Insurance
  type: http
  seq: 13

http:
  method: DELETE
  url: "{{baseUrl}}/api/core/patients/1/insurances/7"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

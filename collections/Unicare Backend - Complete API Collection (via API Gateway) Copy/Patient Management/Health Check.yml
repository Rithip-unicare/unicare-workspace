info:
  name: Health Check
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/api/core/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: check mr
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/api/core/patients/checkmr/MR-1-000007?hospitalId=1"
  params:
    - name: hospitalId
      value: "1"
      type: query
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

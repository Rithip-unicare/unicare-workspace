info:
  name: Unblock Patient
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/api/core/patients/:patientId/unblock"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: patientId
      value: "7"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: add doctor slots
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/doctors/slots/add"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
          "location":"ALB1",
          "starDate":"2026-07-07",
          "endDate":"2026-07-30"
          // "centerId":"1",
          // "doctorId":"1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

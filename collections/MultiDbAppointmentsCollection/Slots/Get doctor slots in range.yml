info:
  name: Get doctor slots in range
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/doctors/slots"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "doctorId": 1,
        "doctorLocation": "ALB5",
        "fromDate": "2026-07-16",
        "toDate": "2026-07-19"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

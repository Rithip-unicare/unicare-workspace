info:
  name: Get Doctors
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/doctors/ALB1/1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

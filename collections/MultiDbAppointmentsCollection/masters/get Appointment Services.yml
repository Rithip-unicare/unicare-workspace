info:
  name: get Appointment Services
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/masters/appointment-services"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

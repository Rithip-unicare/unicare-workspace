info:
  name: Departments
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseUrl}}/masters/departments"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

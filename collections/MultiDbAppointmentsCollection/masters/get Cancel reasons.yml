info:
  name: get Cancel reasons
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/masters/cancel-reasons"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

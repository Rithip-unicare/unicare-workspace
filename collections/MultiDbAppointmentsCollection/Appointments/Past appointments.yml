info:
  name: Past appointments
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/appointments/past/ALB3/1?page=0&size=20"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: page
      value: "0"
      type: query
    - name: size
      value: "20"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: current appointments
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/appointments/current/ALB3/1?page=0&size=20"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: page
      value: "0"
      type: query
    - name: size
      value: "20"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Delete schedule Break
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/doctor-break/schedule-break/1921/ALB1"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
